// API Configuration
const config = {
    // Replace SERVER_IP with your actual server IP when deploying
    API_URL: process.env.REACT_APP_API_URL || 'http://localhost:8000',
};

export default config; 